generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model cartera {
  id     Int     @id @default(autoincrement())
  nombre String?
  deuda  BigInt?
}
